{
  "buildCommand": "npx vite build",
  "outputDirectory": "renderer/dist",
  "installCommand": "npm install && cd renderer && npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
